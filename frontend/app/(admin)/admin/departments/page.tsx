"use client";

// 부서 관리 화면 골격(FND-002). 트리·등록(3단계)·비활성화 UI는 후속 단계에서 구현.
export default function DepartmentsAdminPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">부서 관리</h1>
      <p className="mt-2 text-sm text-gray-600">조직 트리(최대 3단계)·등록·비활성화(후속 구현)</p>
    </div>
  );
}
