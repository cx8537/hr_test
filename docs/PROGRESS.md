# HR_Test_05 — 진행 상태 (PROGRESS)

이 파일은 Sidabari4Loop 자율 루프의 상태 원본이다. 매 턴 이 파일의 "다음 할 일"을 읽고 한 단계를 수행한 뒤, "진행 로그"에 한 줄을 append 하고 "다음 할 일"을 갱신한다. 상세 작업 순서는 `docs/BUILD_ORDER.md` 참고.

## 다음 할 일
- Phase 8-1(DOC, TDD): 문서 공개범위 판정 순수 로직(DOC-005). `document.domain`에 (공개범위, 요청자, 관여자집합)→열람 허용/거부 판정. 관여자=상신자+결재자(+합의/대결/수임)+시스템관리자. 공개범위 유형(전체공개/부서공개/관여자한정 등 `@docs/06-document-archive.md` 기준) 분기. 민감 양식(휴가·근태)·반려·회수 문서는 관여자/관리자만(CLAUDE.md). Spring 비의존 POJO + 부정 테스트(비관여자 거부·관여자 허용·SYS_ADMIN 허용·부서 매칭). verify: `gradlew test` 통과. (실DB OPEN[01].)

## 미해결
OPEN[01]: 로컬 PostgreSQL(superuser/`hr` 계정) 및 MinIO 자격증명이 없어 `application-local.yml`을 채울 수 없다. 사람이 `backend/src/main/resources/application-local.yml`(예시 파일 복사)에 DB 비밀번호·JWT 시크릿·MinIO 키·부트스트랩 관리자 초기 비밀번호를 입력해야 백엔드 실기동·Flyway 적용·`/api/health` 실연결 검증이 가능하다. PostgreSQL 18(5432)·MinIO(9000)는 가동 중 확인됨.

## 진행 로그
- 2026-06-19: 셋업 턴. CLAUDE.md·docs 확인, PROGRESS.md / BUILD_ORDER.md 생성. 기능 개발은 미시작.
- 2026-06-19: Phase 0-1 완료. `/backend` Spring Boot 3.5.15(JDK21, Gradle Groovy DSL) 골격 생성 — 의존성(web/data-jpa/flyway/postgresql/validation/security/jjwt 0.12.6), `application.yml`에 KST·`ddl-auto=validate`·환경변수 외부화(DB/JWT/MinIO/부트스트랩), JVM 타임존 KST 고정. DB 의존 기본 테스트는 스모크 단위 테스트로 교체(컨텍스트 로드 검증은 DB 연결되는 0-3 이후). `gradlew build` 성공. OPEN[01] 해소: jjwt + Gradle Groovy DSL 확정, Lombok 미사용.
- 2026-06-19: Phase 0-2 완료. `/frontend` Next.js 16.2.9(App Router, CSR, TypeScript, Tailwind v4) 골격 생성 — `(portal)/dashboard`·`(admin)/admin` 라우트 그룹, `lib/api.ts`(fetch 클라이언트, 백엔드 URL env 외부화), `.env.local.example`. 테스트 러너 Vitest + Testing Library 설정, 스모크 테스트 추가. `npm run build` 성공(/, /admin, /dashboard 라우트 생성) + `npm run test` 통과.
- 2026-06-19: Phase 0-3 완료(코드·문서). 개발 CORS+Security 설정(`SecurityConfig`, 골격 단계 permitAll·Phase1에서 RBAC 교체 예정), 헬스 엔드포인트(`HealthController` /api/health, DB ping), `application.yml`에 `app.cors.allowed-origins` 추가, `application-local.yml.example` 및 `.gitignore`(local yml 제외), `docs/INFRA_SETUP.md`(PostgreSQL/MinIO/기동순서) 작성. `gradlew build` 성공. PostgreSQL18(5432)·MinIO(9000) 가동 확인. 실DB 연결 헬스체크는 자격증명 미보유로 보류 → OPEN[01].
- 2026-06-19: Phase 0-4 완료. Flyway 베이스라인 `db/migration/V1__init.sql` 작성 — `btree_gist` 확장(예약 EXCLUSION용), `set_updated_at()` 트리거 함수, 공통 컬럼 규약(created_at/updated_at TIMESTAMPTZ KST, version 낙관적 잠금) 및 상태값 enum 목록(VARCHAR+CHECK 전략, JPA @Enumerated(STRING) 친화)을 11-data-model 기준 주석으로 정의. 실제 테이블은 모듈별 후속 마이그레이션에서 추가. `gradlew build` 성공(리소스 패키징). 실Flyway 적용·`ddl-auto=validate` 검증은 DB 자격증명 확보 후 → OPEN[01].
- 2026-06-19: Phase 1-1 완료(TDD). FND 도메인 순수 로직 4종 + 단위 테스트 19개. `auth.domain.PasswordPolicy`(FND-003 AC3: 8자·영문+숫자, 경계값/null 부정), `org.domain.DepartmentTree`(FND-002 AC1: 3단계 제한, 4단계 거부), `auth.domain.RbacPolicy`+`Role`/`ScopeType`/`RoleAssignment`/`AccessRequest`(FND-006: 역할×범위 판정, SYS_ADMIN 전역, 다중범위 매칭, 범위외 거부), `auth.domain.TokenVersionValidator`(FND-004: 활성+버전일치). Red(컴파일 실패) 확인 후 구현 → `gradlew test` Green. 전부 Spring 비의존 POJO.
- 2026-06-19: Phase 1-2 완료(엔티티·SQL, 통합 테스트 보류). 공통: `common.domain.EntityStatus`, `common.entity.BaseEntity`(@Version, JPA Auditing created/updated_at), `config.JpaConfig`(@EnableJpaAuditing). 엔티티: `org.entity.Department`/`Employee`, `auth.entity.EmployeeRoleScope`(모듈 결합 최소화 위해 FK는 Long ID 보유). Repository 3종. Flyway `V2__fnd_core.sql`(유니크·level CHECK·status CHECK·트리거·인덱스). `SecurityConfig`에 BCrypt `PasswordEncoder` 빈 추가. `auth.AdminSeeder`(ApplicationRunner: 최초 SYS_ADMIN+본사 부서 시드, must_change_password=true). 통합 테스트 `EmployeeRepositoryIT`는 작성 후 @Disabled(OPEN[01]). `gradlew build` 성공(컴파일·단위테스트·@Disabled 스킵).
- 2026-06-19: Phase 1-3 완료(TDD+Mockito). `auth.jwt.JwtTokenProvider`(jjwt + Clock 주입, Access=subject+만료+tv만, 역할 제외 FND-003 AC4) — 순수 단위 테스트 6개(만료/위조/클레임/tv) Red→Green. `RefreshToken` 엔티티/Repository + Flyway `V3__refresh_token.sql`. `ClockConfig`(KST Clock 빈), `JwtConfig`(JwtTokenProvider 빈). `AuthService`(로그인: BCrypt 매칭, 비활성 거부 AC2, Access+Refresh 발급+해시 저장, mustChangePassword 반환; refresh 갱신) — Mockito 단위 테스트 4개(정상/비활성/비번불일치/없는ID). `AuthController`(/api/auth/login,/refresh) + `AuthDtos`. `gradlew build` 성공(단위테스트 통과). 인증 통합 e2e는 DB 필요 → OPEN[01].
- 2026-06-19: Phase 1-4 완료(TDD). `auth.JwtAuthenticationFilter`(OncePerRequestFilter): Bearer 파싱→jwt 검증→매 요청 계정활성+토큰버전 DB 조회 확인(`TokenVersionValidator`)→무효 시 401 즉시 차단, 유효 시 SecurityContext에 사용자ID 설정. 필터 단위 테스트 6개(유효/토큰없음/만료/위조/버전불일치/비활성→401). `SecurityConfig`를 STATELESS + 로그인·갱신·헬스 permitAll + 그 외 authenticated로 교체, JWT 필터 등록. 로그아웃(`Employee.incrementTokenVersion`, `AuthService.logout`: 토큰버전↑+refresh revoke, `AuthController` /api/auth/logout) 추가. Mockito 중첩 stubbing 실수 수정 후 `gradlew build` Green.
- 2026-06-19: Phase 1-5 완료(Mockito). `auth.AuthorizationService`(역할·범위 매 요청 DB 조회→`RbacPolicy` 판정, `checkAllowed`는 거부 시 `AccessDeniedException`; 토큰 비의존이라 권한 변경 즉시 반영 FND-006 AC4). 단위 테스트 5개(지정범위 허용/범위외 거부/SYS_ADMIN 전역/checkAllowed AccessDenied/허용). `config.GlobalExceptionHandler`(@RestControllerAdvice: AccessDenied→403, AuthenticationException·JwtException→401, 검증 실패→400) + 단위 테스트 2개. `gradlew build` 성공. 1-6은 백엔드 관리 API(1-6)와 프론트 화면(1-7)으로 분리 진행 예정.
- 2026-06-19: Phase 1-6 완료(백엔드, Mockito). 부서 관리(`org.service.DepartmentService`: 3단계 강제·코드 중복 거부·비활성화 소프트삭제), 임직원 관리(`org.service.EmployeeService`: emp_no/login_id 중복 거부·PasswordPolicy 검증+BCrypt·신규 must_change_password=true·비활성화·비밀번호 리셋), 역할범위(`auth.service.RoleScopeService`: 부여/회수, scopeId 정합). 엔티티 상태 메서드(`Department.deactivate`, `Employee.deactivate/resetPassword`). 컨트롤러 3종(`/api/admin/departments|employees|role-scopes`, 전부 `AuthorizationService.checkAllowed(SYS_ADMIN)` 보호). `GlobalExceptionHandler`에 IllegalArgument→400·IllegalState→409 추가. Service 단위 테스트 14개(Department 4/Employee 6/RoleScope 4). `gradlew build` 성공.
- 2026-06-19: Phase 1-7 완료(프론트). `lib/auth.ts`(Access/Refresh 토큰 localStorage 보관), `lib/api.ts`에 Authorization 자동 첨부 + `login`/`logout`. `components/login-form.tsx`(주입형 onSubmit), `app/login`(성공 시 mustChangePassword→/change-password, 아니면 /dashboard), `components/require-auth.tsx` 가드 + `(admin)/layout`(메뉴+가드), 관리 화면 골격(employees/departments/role-scopes), `change-password` 골격. 프론트 테스트 5개(auth 토큰 2 + 로그인폼 2 + smoke 1) 통과, `npm run build` 성공(라우트 9개). 연결된 Chrome으로 `/login` 렌더 확인(폼·입력·버튼) 후 dev 서버 정리. → Phase 1(FND) 완료. 다음은 Phase 2(서명 키).
- 2026-06-19: Phase 2-1 완료(TDD). `signature.domain.SignatureVerifier`(순수): RSA-2048 / SHA256withRSA 서명 검증, PEM·Base64 SPKI 공개키 파싱(브라우저 Web Crypto RSASSA-PKCS1-v1_5 호환), 입력 오류 시 false. 단위 테스트 5개(유효 서명 통과 AC1 / 변조 데이터·위조 서명 실패 AC2 / PEM 파싱 / 잘못된 입력). 테스트 키쌍은 `KeyPairGenerator`로 생성. `gradlew build` 성공.
- 2026-06-19: Phase 2-2 완료(Mockito). `signature.domain.SignatureKeyStatus`(ACTIVE/REVOKED), `signature.entity.SignatureKey`(id=공개키ID, employeeId, publicKey text, status, revokedAt — 공개키만 저장) + Flyway `V4__signature_key.sql`. `SignatureKeyService`(발급=공개키만 저장 FND-007 AC1, 폐기=REVOKED 보존·삭제금지 FND-009, 재발급=기존 활성 폐기+새 발급, getById/findActive). `SignatureKeyController`(/api/signature-keys: 본인 발급·재발급·조회, SYS_ADMIN 폐기). 단위 테스트 3개(발급 ACTIVE/폐기 보존·delete 미호출/재발급). `gradlew build` 성공.
- 2026-06-19: Phase 2-3 완료(교차 검증). 프론트 `lib/crypto.ts`(Web Crypto RSASSA-PKCS1-v1_5/SHA-256/2048: 키 생성, 공개키 SPKI·개인키 PKCS8 Base64 export, 서명/검증) + 프론트 테스트 3개(@vitest-environment node: 생성→서명→검증, 변조 실패, export). 백엔드 `SignatureVerifierWebCryptoCompatTest`: 실제 Web Crypto(Node webcrypto)로 생성한 고정 공개키·서명 벡터를 `SignatureVerifier`가 검증(SHA256withRSA ↔ RSASSA-PKCS1-v1_5 교차 확인) + 변조 실패. `npm run test`(8개) + `gradlew build`(GRADLE_EXIT=0) 통과. → Phase 2(서명 키) 완료. 다음은 Phase 3(결재 핵심 흐름).
- 2026-06-19: Phase 3-1 완료(TDD). `approval.domain` 결재 흐름 엔진 — `StepType`(SEQUENTIAL/PARALLEL/CONSENT)·`MemberState`(PENDING/APPROVED/REJECTED/CONSENT_REJECTED/SKIPPED)·`DocumentStatus`(DRAFT/IN_PROGRESS/ON_HOLD/APPROVED/REJECTED/WITHDRAWN)·`StepMember`/`Step` 레코드, `ApprovalFlowEngine`(순수): `evaluate`(반려→REJECTED, 합의거부→ON_HOLD, 전 단계 완료→APPROVED, 그외 IN_PROGRESS)·`isStepActive`(순차 순서 강제). 단위 테스트 12개(AP-010 순차/AP-011 병렬 전원·부분/AP-012 1인반려/AP-013 합의 보류·철회·전원동의/반려우선/전결 SKIPPED). `gradlew build`(EXIT 0) 성공.
- 2026-06-19: Phase 3-2 완료(TDD). `approval.domain.PrerogativeLineGenerator`(전결: 기본 결재선→전결 단계까지만, null이면 전체, 범위 초과 거부 AP-020 AC1) + 단위 테스트 4개. `ExpenseLine`/`ExpenseCalculator`(금액=수량×단가 BigDecimal AP-041 AC1, 합계=Σ AC2, 음수 수량/단가 거부, 빈 행 합계 0) + 단위 테스트 5개. `gradlew build`(EXIT 0) 성공.
- 2026-06-19: Phase 3-3 완료(TDD). `approval.domain.LeaveDaysCalculator`(순수): 영업일 기준(토·일 제외 AC1), 공휴일 집합 주입 제외(AC3), 반차 0.5일·단일 날짜만(AC2), 종료<시작 거부(AC4), BigDecimal. 단위 테스트 8개(평일5/주말제외/공휴일제외/반차0.5/반차복수거부/역전거부/단일종일/주말만0). `gradlew build`(EXIT 0) 성공.
- 2026-06-19: Phase 3-4 완료(엔티티·SQL). `approval.domain.FormType`(EXPENSE/LEAVE/ASSET_REQ/GENERAL). 엔티티 `ApprovalDocument`(formType/title/drafterId/draftDeptId 스냅샷/status DocumentStatus/currentRound, changeStatus·nextRound), `ApprovalLineSnapshot`(documentId/round/stepNo/approverId/stepType/state MemberState/actedAt, act()), `Holiday`(date unique/name). Flyway `V5__approval_core.sql`(form_type/status/step_type/state CHECK, FK, 인덱스, 트리거). Repository 3종(스냅샷 round 순서 조회, 공휴일 기간 조회). `gradlew build`(EXIT 0) 성공. 통합 테스트는 OPEN[01].
- 2026-06-19: Phase 3-5 완료(Mockito). `signature.service.SignatureValidationService`(공개키ID 조회→SignatureVerifier). `approval.service.ApprovalService`: submit(결재선→스냅샷 복사 고정, IN_PROGRESS AP-001/002), approve(차례 검증 isStepActive→서명 필수 AP-034·검증→act APPROVED→evaluate), reject(사유 필수 AP-031→act REJECTED→evaluate); 스냅샷→Step 변환 후 흐름 엔진 조립. 단위 테스트 9개. currentLine을 documentId 파라미터화 수정 후 `gradlew build`(EXIT 0) 성공.
- 2026-06-19: Phase 3-6 완료(Mockito). `ApprovalDocumentRepository.findWithLockById`(@Lock PESSIMISTIC_WRITE, AP-033 동시성). `ApprovalService` approve/reject가 락 조회+상태 재검증(`requireInProgress`: 진행중만, 종결 상태 거부 AC1). 회수 `withdraw`(AP-030: 상신자만, 무승인만 — 1인이라도 승인 시 거부, 진행중/보류만) 추가. 단위 테스트 4개 추가 → 총 13개. `gradlew build`(EXIT 0) 성공. 실제 동시 락 일관성 검증은 PostgreSQL+멀티스레드 필요 → OPEN[01](비관적 락은 @Lock 어노테이션으로 적용됨).
- 2026-06-19: Phase 3-7 완료(Mockito). `ApprovalService.resubmit`(AP-032: 반려 문서만·상신자만, `nextRound()`로 라운드 증가 후 새 라운드 결재선 스냅샷 1단계부터 생성, 이전 라운드 보존, IN_PROGRESS 재개). 단위 테스트 3개 추가 → 총 16개. `gradlew build`(EXIT 0) 성공. → Phase 3 핵심 흐름(3-1~3-7) 완료. 다음은 Phase 4(위임·대결·양식).
- 2026-06-19: Phase 4-1 완료(TDD+Mockito). `approval.domain.DelegationResolver`(순수): (원결재자,처리자,대결/위임 링크)→처리권한(SELF/DEPUTY/MANDATEE), 위임자 명의 우선 — 단위 테스트 5개. `Delegation`/`Mandate` 엔티티 + Flyway `V6__delegation_mandate.sql`(테이블 2 + `approval_line_snapshot.acted_by_id` 추가) + Repository(findByActiveTrue). `ApprovalLineSnapshot.act`에 actedById(실제 처리자) 추가. `ApprovalService` approve/reject가 `requireProcessableMember`로 본인/유효 대결·위임 처리 허용(원 결재자 보존+actedById 기록, 서명은 actor 본인 키). 대결/위임 처리 테스트 3개 추가 → ApprovalService 19개. `gradlew build`(EXIT 0) 성공.
- 2026-06-19: Phase 7-3 완료(RSV 컨트롤러·서비스·프론트). `ResourceService`(자원 등록 RSV-001, 비활성화 시 미래 활성 예약 자동 취소 RSV-007 AC1 — Clock+`findByResourceIdAndStatusAndEndAtAfter`, 알림 Phase 9; 거점/예약→거점 조회 RBAC 헬퍼). `ReservationService.reserverIdOf` 추가(취소 본인 판정). `ReservationController`(`/api/reservations` 예약=인증사용자 즉시확정·취소=본인 자유/타인 거점관리자 RBAC, `/api/resources` 등록·비활성화 거점관리자). DTO. 프론트: `lib/reservation-view.ts`(순수 overlaps/hasConflict 맞닿음 허용 RSV-003·needsVehicleFields RSV-004) + `lib/api.ts` 예약 래퍼 + `app/(portal)/reservations`(유형별 차량 필드 조건 렌더, RequireAuth). 백엔드 Service 단위 테스트 2개(자원 등록·비활성화 미래예약 취소), 프론트 +6개. `gradlew test`(EXIT 0) + `npm run test`(33개) + `npm run build`(라우트 14개) 통과. → Phase 7(RSV) 완료. 다음 Phase 8(DOC). 실서버/EXCLUSION OPEN[01].
- 2026-06-19: Phase 7-2 완료(RSV 엔티티·EXCLUSION·서비스, Mockito). enum `ResourceType`(VEHICLE/MEETING_ROOM)·`ReservationStatus`(ACTIVE/CANCELLED). 엔티티 `Resource`(거점·유형·차량/회의실 속성·status), `Reservation`(자원·예약자·기간[start,end)·목적/인원/비고·차량 행선지·운전자 RSV-004·취소사유/취소자), `ResourcePhoto`. Flyway `V11__reservation.sql` — 기간 CHECK + **EXCLUSION 제약**(`EXCLUDE USING gist (resource_id WITH =, tstzrange(start_at,end_at,'[)') WITH &&) WHERE status='ACTIVE'`, 맞닿음 허용·동시 한 건 RSV-003 AC2/AC3). Repository 3종(겹침/미래예약 조회). `ReservationService`(즉시예약=`TimeRangeOverlap` 사전검사 후 저장 RSV-002, DB 제약이 동시성 최종 보장; 취소=본인 자유·타인은 사유 필수 RSV-005, 알림은 NOTI Phase 9). Service 단위 테스트 7개(즉시확정·겹침거부·맞닿음허용·차량정보·본인취소·관리자 사유필수·관리자 사유포함). `gradlew test`(EXIT 0) 통과. 실DB EXCLUSION 동시성 검증 OPEN[01].
- 2026-06-19: Phase 7-1 완료(RSV, TDD). `reservation.domain.TimeRangeOverlap`(순수): 반열림 구간 [start,end) 겹침 판정 — `aStart<bEnd && bStart<aEnd`, 경계 맞닿음(끝=다음 시작)은 겹침 아님(RSV-003 AC2), 포함·부분겹침·동일구간 충돌(AC1), 종료≤시작 거부. Spring 비의존 POJO + 경계값 단위 테스트 8개(분리·맞닿음 전/후·부분겹침·완전포함·동일구간·역전거부·길이0거부). `gradlew test`(EXIT 0) 통과. 동시성(EXCLUSION)은 7-2·OPEN[01].
- 2026-06-19: Phase 6-4 완료(프론트 비품 화면, Vitest). `lib/api.ts`에 비품 래퍼(품목/개체 등록·상태변경·폐기·입출고·현재수량). `lib/asset-view.ts`(순수): `previewQuantity`(입고 가산·출고 차감, 음수 거부 AST-003 AC3, 백엔드 동일 규칙)·`statusLabel`·`isDiscarded`(폐기 표시 AST-004). `app/(portal)/assets` 비품 화면 골격(수량 입출고 미리보기 폼, RequireAuth). 단위 테스트 +6개(입고/출고/음수거부/음수수량/라벨/폐기여부). `npm run test`(27개) + `npm run build`(라우트 13개, EXIT 0) 통과. → Phase 6(AST) 완료. 다음 Phase 7(RSV). 실서버 OPEN[01].
- 2026-06-19: Phase 6-3 완료(AST 서비스·컨트롤러, Mockito). `asset.service.AssetService`(품목 등록=관리유형 필수 AST-001 AC1, 수량유형이면 QuantityAsset 동반 생성; 개체 등록=개체유형 검증+관리번호 중복 거부 AST-002 AC1; 상태변경 덮어쓰기·폐기 보존 AST-004; 입출고 기록=수량유형 검증+`StockCalculator`로 음수 사전 거부 AST-003 AC3 후 저장 AC1; 현재 수량 파생 AC2; Clock 주입; RBAC용 거점ID 헬퍼). `AssetPhotoService`(MinIO 업로드/목록/다운로드, 개체·품목 owner). `asset.service.AssetResourceCounter`(`LocationResourceCounter` 구현, 거점별 비폐기 개체+수량 합 → 거점 비활성화 판정 연결 LIFE-C1; 기본 0건 빈 대체). `AssetController`(`/api/assets`: 비품관리자 거점범위 RBAC `(ASSET_MANAGER, LOCATION, locId)`, 품목/개체/상태/폐기/입출고/수량) + DTO. 결재 연동 없음(AST-006). Service 단위 테스트 7개(유형필수·수량 QuantityAsset 생성·관리번호중복·폐기보존·현재수량 파생·출고음수거부·정상입고). `gradlew test`(EXIT 0) 통과. 실DB/MinIO OPEN[01].
- 2026-06-19: Phase 6-2 완료(AST 엔티티·SQL). enum `AssetManagementType`(INDIVIDUAL/QUANTITY)·`IndividualAssetStatus`(USING/STORAGE/REPAIR/DISCARDED)·`AssetPhotoOwnerType`(ITEM/INDIVIDUAL). 엔티티: `AssetItem`(거점·품목명·관리유형), `IndividualAsset`(관리번호 유니크·상태 덮어쓰기 changeStatus/discard·취득일 AST-002/004), `QuantityAsset`(품목당 1건, 수량 파생 AST-003), `StockTransaction`(IN/OUT·수량·발생시각 이력), `AssetPhoto`(개체/품목 다중, MinIO 메타). Flyway `V10__asset.sql`(유형·상태 CHECK, FK location/employee, 관리번호 유니크, 입출고 인덱스). Repository 5종(품목 거점별·관리번호 중복·이력 시각순·사진 owner별). `gradlew test`(EXIT 0) 통과. 서비스/통합은 6-3·OPEN[01].
- 2026-06-19: Phase 6-1 완료(AST, TDD). `asset.domain.StockTransactionType`(IN/OUT), `asset.domain.StockCalculator`(순수): 입출고 이력을 시간순 누적해 현재 수량 산출 — 현재수량=입고합−출고합(AST-003 AC2), 출고로 음수가 되면 즉시 거부(AC3 재고부족), 음수 수량 입력 거부, 빈 이력 0. `StockEntry(type, quantity)` 레코드. Spring 비의존 POJO + 경계값 단위 테스트 7개(빈0·입고누적·입고합빼기출고합·정확히0·출고음수거부·중간재고부족거부·음수입력거부). `gradlew test`(EXIT 0) 통과.
- 2026-06-19: Phase 5-3 완료(프론트 거점 현황, Vitest). `lib/api.ts`에 거점 래퍼(활성 목록·생성·좌표 수정·담당자 지정·사진 업로드 multipart/목록). `lib/location-view.ts`(순수): `hasCoordinates`·`toMarkers`(목록 데이터셋→지도 마커, 좌표 없는 거점 제외, 동일 원본 파생 LOC-005 AC2). `components/location/location-views.tsx`(목록/지도 토글, 지도 API 키 미설정 시 좌표 목록 대체) + `app/(portal)/locations`(활성 거점 조회·렌더, RequireAuth). 단위 테스트 +2개(hasCoordinates/마커 변환). `npm run test`(21개) + `npm run build`(라우트 12개, EXIT 0) 통과. → Phase 5(LOC) 완료. 지도 API 키·실서버 OPEN[01].
- 2026-06-19: Phase 5-2 완료(LOC 엔티티·CRUD·사진, Mockito). `location.entity.Location`(코드 유니크·이름·주소·좌표 lat/lng NUMERIC(9,6)·연락처·팩스·유형·담당자 managerId·status, update/updateCoordinates/assignManager/deactivate) + `LocationPhoto`(다중 사진 메타, MinIO objectKey) + Flyway `V9__location.sql`. Repository 2종(existsByLocationCode·findByStatus·findByLocationId). `LocationService`(생성=코드중복 거부 LOC-001 AC1, 좌표 LOC-002, 담당자 LOC-003, 비활성화=`LocationResourceCounter`로 영향 건수 조회→`LocationDeactivation.evaluate` 판정, 자원 잔존 시 거부 LOC-006 AC1, findActive). 모듈 경계용 `LocationResourceCounter` 인터페이스 + 기본 0건 빈(`@ConditionalOnMissingBean`, AST/RSV 도입 시 교체). `LocationPhotoService`(MinIO 업로드/목록/다운로드/삭제, 백엔드 경유). `LocationController`(`/api/locations`: 생성=SYS_ADMIN, 거점별 변경·사진=거점관리자 범위 RBAC `(LOCATION_MANAGER, LOCATION, id)`, 사진 백엔드 스트리밍) + DTO. Service 단위 테스트 6개(생성·코드중복거부·좌표·담당자·자원잔존 비활성화거부·자원없음 허용). `gradlew test`(EXIT 0) 통과. 실DB/MinIO OPEN[01].
- 2026-06-19: Phase 5-1 완료(LOC, TDD). `location.domain.LocationDeactivation`(순수): 거점 비활성화 가능 판정 — 소속 활성 자원 잔존 시 거부(LOC-006 AC1/LIFE-C1 AC1), 영향 건수(활성 자원·미래 예약) 동반 산출해 사전 경고로 노출(LIFE-C1 AC2), 음수 입력 거부. `Decision(allowed, activeResourceCount, futureReservationCount, reason)` 레코드. Spring 비의존 POJO + 경계값 단위 테스트 6개(자원0허용/자원잔존거부/경계0/허용시 예약건수노출/거부시 영향건수노출/음수거부). `gradlew test`(EXIT 0) 통과.
- 2026-06-19: Phase 4-4 완료(백엔드 결재 REST, Mockito). `ApprovalController`(`/api/approval/documents`: 상신 AP-001, `/{id}/approve` AP-010/034, `/{id}/reject` AP-031, `/{id}/withdraw` AP-030) + `ApprovalDtos`(SubmitRequest=헤더+결재선+양식별 본문, ApproveRequest/RejectRequest/Document·StatusResponse). 상신 시 `ApprovalService.submit`로 결재선 스냅샷 고정 후 formType별 `FormBodyService` 분기 저장(지출/휴가/비품/일반), 승인/반려/회수는 서비스 위임(`@AuthenticationPrincipal`로 actorId, 처리권한·서명은 서비스 판정). 컨트롤러 단위 테스트 6개(지출 상신+본문저장/일반 상신+본문저장/승인 서명전달/반려 사유/회수/회수는 본문서비스 미호출). `gradlew test`(EXIT 0) 통과. → Phase 4(AP) 완료. 다음 Phase 5(LOC). 실서버 연동 OPEN[01].
- 2026-06-19: Phase 4-3b 완료(프론트 결재 작성·결재함, Vitest). `lib/approval-form.ts`(순수): `expenseAmount`/`expenseTotal`(AP-041), `leaveDays`(AP-042 영업일·공휴일·반차, 백엔드 LeaveDaysCalculator와 동일 규칙), `isGeneralBodyValid`(AP-045), `signaturePayload`(`doc:{id}:r{round}:{approverId}` — 백엔드 ApprovalService와 동일 포맷). `lib/api.ts`에 결재 래퍼(submit/approve/reject/withdraw + 첨부 업로드 multipart·다운로드 URL, presigned 미사용). 양식별 작성 폼 컴포넌트 4종(`components/approval/`: 지출 행추가·금액자동·합계, 휴가 일수 자동표시, 비품 수령거점·예상합계, 일반 본문필수) + `app/(portal)/approval/new`(양식 선택) + `app/(portal)/approval`(결재함 상신/수신/완료 탭 골격, RequireAuth 가드). 단위 테스트 +10개(approval-form: 금액/합계/음수거부/휴가 5케이스/본문/서명 payload). `npm run test`(19개) + `npm run build`(라우트 11개, EXIT 0) 통과. 작성 폼 실렌더는 인증·백엔드 필요로 OPEN[01]. 프론트 래퍼 대상 결재 REST 컨트롤러는 미존재 → Phase 4-4.
- 2026-06-19: Phase 4-3a 완료(백엔드 첨부, TDD+Mockito). AP-040: 첨부 메타 엔티티 `Attachment`(documentId·fileName·contentType·sizeBytes·uploaderId·objectKey) + Flyway `V8__attachment.sql`. 파일 저장 추상화 `common.storage.FileStorage`(put/get/delete) + `MinioFileStorage`(io.minio 8.5.17, `@ConditionalOnProperty(app.minio.enabled=true)` — 자격증명 확보 후에만 빈 등록, presigned 미발급·백엔드 스트림 중계). `AttachmentService`(업로드=상신자만·MinIO 저장 후 메타 기록 AC1, 다운로드=관여자[상신자/결재선 멤버]만 검사 후 스트림 반환, 비관여자 `AccessDeniedException`→403) + `AttachmentController`(multipart 업로드·목록·`InputStreamResource` 다운로드). `ApprovalLineSnapshotRepository.findByDocumentId` 추가. `application.yml`에 `app.minio.enabled` 추가. 단위 테스트 5개(업로드 메타·MinIO 저장/비상신자 거부/상신자·결재선멤버 다운로드/비관여자 거부). `gradlew test`(EXIT 0) 통과. 실 MinIO 연동은 OPEN[01].
- 2026-06-19: Phase 4-2 완료(TDD+Mockito). 결재 양식 4종 본문 + 공휴일 관리(AP-041/042/044/045/043). 엔티티: `GeneralDoc`(본문 NOT NULL), `ExpenseDoc`+`ExpenseDocLine`(금액 컬럼 없음=수량×단가 계산), `LeaveDoc`(days 산출 저장), `AssetReqDoc`+`AssetReqDocLine`(수령거점 필수) + Flyway `V7__approval_forms.sql`(6테이블, NUMERIC 단가·CHECK 음수금지·트리거). `approval.service.FormBodyService`(양식별 저장: 지출/비품 금액=`ExpenseCalculator`로 계산·저장 안 함 AP-041 AC1, 휴가 일수=`LeaveDaysCalculator`+공휴일 즉시반영 AP-042 AC1/3, 일반 본문 필수 AP-045 AC1, 비품 수령거점 필수 AP-044). `HolidayService`(AP-043 등록·수정·삭제, 중복 거부, `Holiday.update`) + `HolidayController`(/api/admin/holidays, SYS_ADMIN CRUD `checkAllowed`) + `HolidayDtos`. 단위 테스트 +10개(FormBody 6: 지출 금액자동·일반 본문 필수/저장·휴가 공휴일반영·비품 수령거점 필수/합계, Holiday 4: 등록·중복거부·수정·없는삭제거부). `gradlew test`(EXIT 0) 통과. 본문↔결재 흐름 연동(상신 시 본문 저장)·첨부·실DB는 Phase 4-3/OPEN[01].
